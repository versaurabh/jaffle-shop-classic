version: 2

models:
    - name: expensive_completed
      columns:
        - name: customer_id
          tests:
            - unique
            - not_null
        - name: first_name
        - name: last_name
        - name: expensive_completed
  
    - name: expensive_returned
      columns:
        - name: customer_id
          tests:
            - unique
            - not_null
        - name: first_name
        - name: last_name
        - name: expensive_returned

        